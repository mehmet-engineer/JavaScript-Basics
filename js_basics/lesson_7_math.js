// Math class 
let x = 5;
let pow = Math.pow(x, 2);

let y = 25;
let sqrt = Math.sqrt(y);

let z = -5;
let abs = Math.abs(z);

let min = Math.min(1, 2, 3, 4, 5);
let max = Math.max(1, 2, 3, 4, 5);

let num = 2.4
let round = Math.round(num);
let ceil = Math.ceil(num);
let floor = Math.floor(num);

let PI = Math.PI;
let E = Math.E;

let rad_angle = 0.5;
let sin = Math.sin(rad_angle);
let cos = Math.cos(rad_angle);
let tan = Math.tan(rad_angle);

let len = 1;
let asin = Math.asin(len);
let acos = Math.acos(len);
let atan = Math.atan(len);

let my_num = -1.0;
let sign = Math.sign(my_num);